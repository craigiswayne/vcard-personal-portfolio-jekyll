<section class="timeline">
    {% include timeline/title.html.liquid title="Education" %}
    <ol class="timeline-list">
        {% for education in site.data.education %}
            <li class="timeline-item">
                <h4 class="h4 timeline-item-title">{{ education.institute }}</h4>
                <span>{{ education.dateStart }} — {{ education.dateEnd }}</span>
                <p class="timeline-text">
                    {{ education.description }}
                </p>
            </li>
        {% endfor %}
    </ol>
</section>